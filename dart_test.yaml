paths: ["test/realtime/unit"]

tags:
  integration:
  proxy:

presets:
  proxy:
    paths: ["test/realtime/integration/proxy"]
  integration:
    paths: ["test/realtime/integration"]
  all:
    paths: ["test"]
